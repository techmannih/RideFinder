import CreateDeal from "../../components/Deal/CreateDeal";
import React from "react";

const create = () => {
    return (
        <div>
          <CreateDeal />
        </div>
      );
    }

export default create;
